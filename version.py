#!/usr/bin/env python3

# The shebang line(above) in any script determines the script's 
# ability to be executed like a standalone executable 
# without typing python beforehand in the terminal or
#  when double clicking it in a file manager 
# (when configured properly). 

import platform 

print ('This is Python version {}'.format(platform.python_version()))

